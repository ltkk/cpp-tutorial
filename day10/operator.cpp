#include <iostream>
using namespace std;

int main(){
    // Toán tử số học
    // int a = 5, b = 9;
    // cout << a << " + " << b << " = " << a + b << endl;
    // cout << a << " - " << b << " = " << a - b << endl;
    // cout << a << " * " << b << " = " << a * b << endl;
    // cout << a << " / " << b << " = " << (float)a / b << endl;
    // cout << a << " % " << b << " = " << a % b << endl;
    // a++;
    // cout << "a++ =" << a << endl;
    // a--;
    // cout << "a-- =" << a << endl;

    // Toán tử gán
    // int number = 5;
    // cout << "number = " << number << endl;
    // number += 5; // <=> number = number + 5;
    // cout << "number = " << number << endl;
    // number -= 5;
    // cout << "number = " << number << endl;
    // number *= 5;
    // cout << "number = " << number << endl;
    // number /= 5;
    // cout << "number = " << number << endl;

    // Toán tử so sánh
    // int a_val = 5, b_val = 9;
    // cout << "a_val == b_val ? " << (a_val == b_val) << endl;
    // cout << "a_val > b_val ? " << (a_val > b_val) << endl;
    // cout << "a_val < b_val ? " << (a_val < b_val) << endl;
    // cout << "a_val != b_val ? " << (a_val != b_val) << endl;
    // cout << "a_val <= b_val ? " << (a_val <= b_val) << endl;

    // Toán tử logic
    int f_val = 10, s_val = -5;
    cout << "f_val > 0 && s_val > 0 ? " << (f_val > 0 && s_val > 0) << endl; 
    cout << "f_val > 0 || s_val > 0 ? " << (f_val > 0 || s_val > 0) << endl; 

    bool is_success = true;
    cout << "!is_success = " << (!is_success) << "\n";
}